import http from 'node:http';import {readFile} from 'node:fs/promises';import path from 'node:path';
const root=path.resolve('dist');const types={'.html':'text/html','.js':'text/javascript','.mjs':'text/javascript','.css':'text/css','.webmanifest':'application/manifest+json','.svg':'image/svg+xml','.png':'image/png','.wasm':'application/wasm'};
http.createServer(async(req,res)=>{try{let p=new URL(req.url,'http://localhost').pathname;if(!p.startsWith('/pdf-library/')){res.writeHead(404).end();return;}p=p.slice('/pdf-library/'.length)||'index.html';const full=path.resolve(root,p);if(!full.startsWith(root+'/'))throw Error();const bytes=await readFile(full);res.setHeader('Content-Type',types[path.extname(full)]||'application/octet-stream');res.end(bytes);}catch{res.writeHead(404).end();}}).listen(8765,'0.0.0.0');
